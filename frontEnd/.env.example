# Copy this file to .env.local and adjust if your backend runs elsewhere.
# NEXT_PUBLIC_ prefix is required — without it Next will not expose the value
# to browser code.

NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
